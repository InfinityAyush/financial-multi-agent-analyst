

# ChromaDB — never commit the vector database, use Docker volumes
chroma_db/

# Large PDF filings — too big for git, mount as volumes
data/sec_filings/*.pdf

# Environment files — never commit API keys
.env
.env.local
.env.production

# Python
__pycache__/
*.pyc
.venv/
dist/

# Node / frontend
node_modules/
frontend/dist/
frontend/.env.local

frontend/.env